/**
 * CORS plugin — configurado em app.ts via @fastify/cors
 * Este arquivo documenta a configuração para referência futura.
 *
 * Configuração atual:
 *   origin: process.env.CORS_ORIGIN (default: http://localhost:5173)
 *   credentials: true (necessário para cookies httpOnly de refresh token)
 */
